{
  "scripts": {
    "test": "npm run compile-tests && npm run run-tests",
    "compile-tests": "node lib/compile-tests.js",
    "run-tests": "mochify lib/run-tests.js --https-server"
  },
  "dependencies": {
    "chai": "^4.2.0",
    "distance-transform": "^1.0.2",
    "fs-extra": "^8.1.0",
    "mochify": "^6.6.0",
    "xmldom": "^0.3.0"
  }
}
